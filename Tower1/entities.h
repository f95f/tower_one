#include <stdio.h>
#include <stdlib.h>

typedef struct player{
    int hp; // vida
    int att; // ataque
    int mp; // magia
} PLAYER;

typedef struct enemy{
    int hp; // vida
    int att; // ataque
} ENEMY;
